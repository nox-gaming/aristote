import WithPost from '../../lib/with-post'

export default WithPost({
    title: 'Who are we ?',
    content: `
Deploying a lot of effort for several years, we act in the shadow:

~~~bash
npm i -g now # one time command
now
~~~
  `
})
